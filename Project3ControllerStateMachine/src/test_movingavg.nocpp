#include "moving_average.h"
#include "hackySerial.h"

int main(){
    Moving25 M;
    USART_Init(0);
    for (int i = 0; i< 200; i++)
    {
        M.set(i);
        print_i_ln((int)(M.get()*100));
    }
    return 0;
}